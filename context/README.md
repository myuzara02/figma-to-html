# context

Reference HTML markup exported from Webflow. Drop the relevant page/section markup
here so it can be referenced while writing and testing code in `scripts/`.

This is reference material only — no executable JavaScript belongs here.
