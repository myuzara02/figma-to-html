# scripts/reference

Reusable snippets and reference code. **Not run directly** — these are patterns to
copy from (e.g. a standard Swiper config, a GSAP section animation).

Code that actually runs / is tested lives one level up in `scripts/`.
